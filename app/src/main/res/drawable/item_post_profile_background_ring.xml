<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <item>
        <!-- create gradient you want to use with the angle you want to use -->
        <shape
            android:shape="oval"
            android:width="32dp"
            android:height="32dp">
            <gradient
                android:angle="180"
                android:endColor="@color/app_main_color_start"
                android:startColor="@color/app_main_color_end" />

        </shape>
    </item>
    <!-- create the stroke for top, left, bottom and right with the dp you want -->
    <item
        android:bottom="2dp"
        android:left="2dp"
        android:right="2dp"
        android:top="2dp">
        <shape
            android:shape="oval"
            android:width="32dp"
            android:height="32dp">
            <!-- fill the inside in the color you want (could also be a gradient again if you want to, just change solid to gradient and enter angle, start, maybe center, and end color) -->
            <solid android:color="#ffffff" />
        </shape>
    </item>

</layer-list>